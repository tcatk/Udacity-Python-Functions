

CHECK COORDINATES:

name.xcor()
name.ycor()

KEEP TURTLE IN BOUNDS LEFT - RIGHT (assuming the canvas is 400 by 400 pixels)
if t.xcor() < -200 or t.xcor() > 200:

import turtle

t = turtle.Turtle()
t.color("lime")
t.width(3)
t.penup()
t.shape("turtle")

for step in range(2000):
    t.forward(1)
    if t.xcor() < -200 or t.xcor() > 200:
        t.left(180)
